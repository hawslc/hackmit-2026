# Copy to client/.env.local. Real extraction/review also needs LLM_PROVIDER=openai + OPENAI_API_KEY
# in the root .env (see ../.env.example).
# Mock API is on unless this is exactly "false". Set to "false" to talk to the real server.
VITE_USE_MOCK_API=false
