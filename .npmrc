save-exact=true
# Release-age cooldown: refuse versions published in the last 7 days.
# Enforced by npm >= 11.10.0; this repo's npm is 10.x, so versions were also
# checked against the registry publish date by hand at install time.
min-release-age=7
