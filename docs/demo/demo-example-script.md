Alright, so today we're going to talk about conditionals in Python. Conditionals let our programs make decisions based on Boolean expressions, which are expressions that evaluate to either True or False.

uhh An if statement runs some code when its condition is true. and umm an else statement runs when none of the previous conditions were true.

Let's look at an example.

Here, we have a score of 87. First, we check if the score is at least 90. Since 87 isn't at least 90, Python moves on to the elif. It checks whether the score is at least 80, which is true, so the grade becomes B.

And importantly, once Python finds a condition that's true, it runs that branch and stops checking the remaining branches.
